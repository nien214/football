Original prompt: the game canvas and the buttons are too large with view in 100% in both portrait and landscape

- Initial review: responsive sizing is split between `public/styles.css` and `updateFieldLayout()` in `public/client.js`.
- Current suspicion: the field width clamp and virtual control size clamps are too aggressive for 100% viewport rendering, especially on coarse-pointer portrait and landscape layouts.
- Implemented: reduced the field-width budget in `updateFieldLayout()` by increasing reserved viewport padding/height and trimming landscape virtual-control side space.
- Implemented: reduced menu panel width/padding, tightened form spacing, and shrank virtual joystick/action/pause button clamps in both portrait and landscape media rules.
- Verification: Playwright MCP checks at 430x932 and 932x430 show the canvas reduced from 410px to 394px in portrait and from 545px to 490px in landscape; action buttons also render smaller.
- Verification: started a CPU match after the layout change and confirmed no console errors through the browser tool.
- Note: the `$WEB_GAME_CLIENT` script could not be run directly because the local Node environment does not have the `playwright` module installed; browser-tool verification was used instead.
- Follow-up iteration: landscape menu was still too dominant inside the field and the virtual controls still read oversized.
- Implemented: reduced menu width/padding/typography again, added a dedicated landscape menu shrink rule, reduced virtual control sizes further, and made the circular action/number buttons explicit with `aspect-ratio`, `appearance: none`, and non-shrinking flex sizing.
- Implemented: trimmed landscape virtual-control side space in `updateFieldLayout()` to give more room back to the field.
- Verification: at 740x360, the menu now measures about 84.6% of field width and 76.9% of field height; primary and secondary menu buttons render at 13px. Browser-tool runtime check still shows no console errors after starting a match.
- Latest iteration: removed the desktop field cap and app-width cap so the pitch scales with the browser window instead of stopping near 1000px wide.
- Latest iteration: added menu auto-fit scaling via `--menu-scale` so the main menu stays inside the field without internal scrolling; `setStatus`, server-connection updates, resize/orientation changes, and field-size changes all reschedule the fit pass.
- Verification: at 1440x900, the pitch now renders at about 1420x880 with menu scale 1. At 740x360, forcing the menu view and waiting two animation frames yields a pitch of about 554x343 and a menu scale of about 0.765, with the menu fully inside the field bounds. At 430x932, the fit logic scales the menu further (about 0.584) so it still stays inside the field.
- HUD fix: moved score/time inside the field instead of placing them above it with a negative `top`, added responsive font clamps, and removed the extra HUD-specific reserved height from `updateFieldLayout()` so the field can still fill the browser.
- Verification: at 1440x900, both score and timer are fully inside the field bounds (score 40px high, time 30px high). At 740x360, both remain inside the field as well, with the score at 24px and time at 20px.
- Fluid resize pass: simplified `updateFieldLayout()` to size directly from the live viewport with only tiny edge padding and a portrait-only reserve for virtual controls; removed the previous state-based vertical reserve logic that was making scaling feel capped/non-fluid.
- Fluid resize pass: reduced body padding to 4px safe-area aware insets and let `.app` span the full browser width/height so the pitch can expand to the browser limits.
- Verification: on a fresh page, the pitch now tracks viewport changes stepwise: 1432x888 at 1440x900, 1116x692 at 1200x700, 892x553 at 900x600, 567x352 at 740x360, and 412x255 at 430x932. Fresh 1440x900 and 740x360 menu checks still show the menu fully inside the field.
- Contain follow-up: added a real safety gutter for the rendered frame by increasing edge padding in `updateFieldLayout()` and restoring larger body insets; also reduced the pitch shadow so the full visual box stays within the browser.
- Verification: at 1440x900 in gameplay, the pitch now sits with about 10px top gap, 24px left/right gap, and 26px bottom gap, while the HUD remains fully inside the field.
- Desktop contain hardening: replaced the old single-source viewport sizing in `updateFieldLayout()` with a conservative viewport read (`min(visualViewport, innerWidth/Height, documentElement client size)`), then based the field budget on actual CSS body padding plus explicit field visual bleed margins.
- Verification: after the contain hardening, gameplay now leaves about 56.6px bottom gap at 1440x900, 56.5px at 1440x820, 56.5px at 1440x740, and 56.5px even at 1440x580; menu view still fits at 1440x900 (`--menu-scale: 1`) and at 430x932 (`--menu-scale: 0.56`).
- Note: Playwright browser console still reports an unrelated existing `401` from `https://football-2kxo.onrender.com/api/state?...` during page checks; this pass did not change the online polling path.
